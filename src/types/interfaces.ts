export interface iCategoryTab {
    categoryId: number,
    index: number,
    value: number,
    label?: string,
}

export interface iPollenItem {
    id: number,
}

export interface iCalendarMonth {
    month: number,
    year: number,
}
