module.exports = {
    // currently only plugins are supported, presets etc. are already set with chayns-toolkit. Example:
    plugins: [
        // ['@babel/plugin-proposal-pipeline-operator', { proposal: 'smart' }]
    ]
};
